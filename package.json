{
  "name": "mcp-tools",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "pnpm typecheck && pnpm test"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
